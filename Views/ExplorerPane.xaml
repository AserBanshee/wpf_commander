<UserControl x:Class="FileVault.Views.ExplorerPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MouseDown="Panel_MouseDown">
    <Border BorderBrush="{Binding BorderColor}" BorderThickness="2" CornerRadius="4"
            Background="#1E1E2E">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Верхняя строка: диск + путь -->
            <Grid Grid.Row="0" Margin="6,6,6,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0"
                          ItemsSource="{Binding Drives}"
                          SelectedItem="{Binding SelectedDrive}"
                          Background="#313244" Foreground="#CDD6F4"
                          BorderBrush="#585B70" FontSize="13"/>

                <TextBox Grid.Column="2"
                         Text="{Binding CurrentPath, Mode=OneWay}"
                         IsReadOnly="True"
                         Background="#181825" Foreground="#A6ADC8"
                         BorderBrush="#585B70" BorderThickness="1"
                         Padding="6,4" FontSize="12"/>
            </Grid>

            <!-- Список файлов -->
            <ListView Grid.Row="1" Margin="6,0,6,6"
                      ItemsSource="{Binding Entries}"
                      SelectedItem="{Binding SelectedEntry}"
                      Background="#181825" Foreground="#CDD6F4"
                      BorderBrush="#585B70" BorderThickness="1"
                      KeyDown="List_KeyDown"
                      SelectionMode="Single">

                <ListView.Resources>
                    <!-- Стиль строки -->
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#CDD6F4"/>
                        <Setter Property="Padding" Value="2,1"/>
                        <EventSetter Event="MouseDoubleClick" Handler="Row_DoubleClick"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#45475A"/>
                                <Setter Property="Foreground" Value="#CDD6F4"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#313244"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Resources>

                <ListView.View>
                    <GridView>
                        <!-- Чекбокс -->
                        <GridViewColumn Width="28">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              Visibility="{Binding IsDirectory,
                                                  Converter={StaticResource DirToCheckVis}}"
                                              Checked="CheckBox_Changed"
                                              Unchecked="CheckBox_Changed"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Имя -->
                        <GridViewColumn Header="Имя" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding IsDirectory,
                                                       Converter={StaticResource DirToIcon}}"
                                                   Margin="0,0,5,0" FontSize="13"/>
                                        <TextBlock Text="{Binding Name}" FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Тип -->
                        <GridViewColumn Header="Тип" Width="70"
                                        DisplayMemberBinding="{Binding EntryType}"/>

                        <!-- Размер -->
                        <GridViewColumn Header="Размер" Width="90"
                                        DisplayMemberBinding="{Binding DisplaySize}"/>

                        <!-- Дата -->
                        <GridViewColumn Header="Изменён" Width="130"
                                        DisplayMemberBinding="{Binding Modified,
                                            StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Border>
</UserControl>
